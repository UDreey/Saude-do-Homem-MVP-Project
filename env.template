# Configuração do MongoDB
# Escolha UMA das opções abaixo:

# OPÇÃO 1: MongoDB Atlas (Recomendado - Cloud)
# 1. Acesse https://www.mongodb.com/cloud/atlas
# 2. Crie uma conta gratuita
# 3. Crie um cluster gratuito
# 4. Crie um usuário de banco de dados
# 5. Adicione seu IP na whitelist (ou use 0.0.0.0/0 para permitir qualquer IP)
# 6. Clique em "Connect" e copie a connection string
# 7. Substitua <password> pela senha do usuário criado
# Exemplo:
# MONGODB_URI=mongodb+srv://usuario:senha@cluster0.xxxxx.mongodb.net/health-on-time?retryWrites=true&w=majority

# OPÇÃO 2: MongoDB Local
# 1. Instale o MongoDB Community Server: https://www.mongodb.com/try/download/community
# 2. Inicie o serviço MongoDB
# 3. Use a string abaixo
# MONGODB_URI=mongodb://localhost:27017/health-on-time

# String de conexão do MongoDB (substitua pela sua)
MONGODB_URI=SUA_STRING_DE_CONEXAO_AQUI

# Secret para JWT (token de autenticação)
# IMPORTANTE: Altere isso em produção para uma string segura e aleatória
JWT_SECRET=jwt-secret-key-change-in-production

# Porta do servidor (opcional, padrão é 3000)
PORT=3000

